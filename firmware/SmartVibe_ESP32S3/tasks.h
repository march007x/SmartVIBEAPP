// ============================================================
//  tasks.h — แยกงานอ่านเซ็นเซอร์กับงานส่งข้อมูลออกคนละคอร์
//
//  ทำไมต้องแยก:
//  เวอร์ชันเดิมเรียก Firebase.RTDB.updateNode() อยู่ใน loop เดียวกับ
//  การอ่านเซ็นเซอร์ ซึ่งเป็น blocking call 150-500 ms
//  → คาบสุ่มตัวอย่างจริงกลายเป็น 20ms หลายครั้งแล้วกระโดด 500ms
//  → ฝั่ง Python ต้อง interpolate ข้ามช่องว่าง เกิด spectral leakage
//    และ alias ทำให้พีคความถี่เพี้ยน
//
//  Core 1 = sampler (ความสำคัญสูง ห้ามมี network call เด็ดขาด)
//  Core 0 = uploader (บล็อกได้เต็มที่ ไม่กระทบ sampling)
//  เชื่อมกันด้วย FreeRTOS queue
// ============================================================
#pragma once
#include <Arduino.h>

bool tasksBegin();
